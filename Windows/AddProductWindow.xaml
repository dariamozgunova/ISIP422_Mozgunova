<Window x:Class="ISIP422_Mozgunova.Windows.AddProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ISIP422_Mozgunova"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Title="Добавить товар" Height="300" Width="350">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Название товара:" 
                   Margin="0,0,10,10" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Name="NameTextBox" 
                 Height="25" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Цена:" 
                   Margin="0,0,10,10" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Name="PriceTextBox" 
                 Height="25" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Количество:" 
                   Margin="0,0,10,10" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Name="QuantityTextBox" 
                 Height="25" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Категория:" 
                   Margin="0,0,10,10" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="3" Grid.Column="1" Name="CategoryComboBox" 
                  Height="25" Margin="0,0,0,20">
            <ComboBoxItem Content="Продукты"/>
            <ComboBoxItem Content="Электроника"/>
            <ComboBoxItem Content="Одежда"/>
            <ComboBoxItem Content="Книги"/>
            <ComboBoxItem Content="Спорттовары"/>
        </ComboBox>

        <StackPanel Grid.Row="8" Grid.ColumnSpan="2" 
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Click="OK_Click" Width="80" Height="30" Margin="5"/>
            <Button Content="Отмена" Click="Cancel_Click" Width="80" Height="30" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
